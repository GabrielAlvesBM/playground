import { Nav } from '@atomic/atm.nav-bar';
import { RootRouter } from '@/root.router';

function App() {
  return (
    <main>
      <Nav />
      <RootRouter />
    </main>
  );
}

export default App;
